Generate a CODING ASSESSMENT for:

TOPIC = "Unsupervised Learning, Clustering, K-means"

--------------------------------------------------
PART 1: BOILERPLATE FILE (coding_boilerplate.py)
--------------------------------------------------

Requirements:

1. Include header with:
   - Python version
   - Required libraries and versions

2. Include:
   - Imports
   - Config block (hyperparameters)
   - Random seed setup
   - Dataset loading (use sklearn dataset)
   - Train/validation/test split
   - Utility functions
   - main() function
   - Validation harness

3. Include 4–8 TODO tasks:

   Each TODO must follow this exact format:

   ```python
   # TODO [T1]: [Short title] 
   # Bloom's Level: [Apply / Analyze / Create] 
   # Difficulty: [Easy / Medium / Hard]  |  Expected lines: ~N 
   # Description: [2–4 sentences: what to implement and why] 
   # Hints: 1. [conceptual]  2. [implementation-specific] 
   # Expected behavior: [output shape, value range, or result] 
   
   # >>> YOUR CODE HERE <<< 
   raise NotImplementedError("TODO T1 not yet implemented") 
   # >>> END YOUR CODE <<< 
   ```

--------------------------------------------------
PART 2: INSTRUCTOR SOLUTION (coding_solution.py)
--------------------------------------------------

- Complete ALL TODOs
- Include comments explaining logic preceeding the code for the TODO
- Ensure code runs end-to-end

--------------------------------------------------
TASK DESIGN REQUIREMENTS
--------------------------------------------------

Include tasks covering:

Apply:
- model creation
- training
- prediction

Analyze:
- evaluate model performance
- detect overfitting
- compare metrics

Create:
- hyperparameter tuning strategy
- feature importance analysis
- improvement suggestions

--------------------------------------------------
VALIDATION REQUIREMENTS
--------------------------------------------------

Boilerplate must include:
- output shape/type checks
- basic assertions
- reproducibility checks

--------------------------------------------------
BENCHMARK REQUIREMENTS
--------------------------------------------------

In solution file, include:
- expected runtime
- expected accuracy range
- hardware assumption (CPU)

--------------------------------------------------
IMPORTANT:
--------------------------------------------------

- Code must be clean and readable
- No hardcoded magic values
- Must run without modification
- Use only standard libraries